# 代码修改规范

## 1. 前提条件：获取最新代码

### 1.1 强制要求
**在进行任何代码修改前，必须确保本地代码是仓库最新版本！**

### 1.2 操作步骤
```bash
# 1. 切换到主分支
git checkout main

# 2. 拉取最新代码
git pull origin main

# 3. 创建新分支进行修改（推荐）
git checkout -b feature/your-feature-name
```

### 1.3 验证方式
- 修改前执行 `git status` 确认工作目录干净
- 修改前执行 `git log --oneline -5` 确认本地代码与远程同步

### 1.4 异常处理
如果本地有未提交的修改：
```bash
# 方式1：临时保存修改
git stash

# 拉取最新代码
git pull origin main

# 恢复修改并合并冲突
git stash pop

# 方式2：直接提交（不推荐，除非修改已完成）
git commit -m "WIP: your changes"
git pull origin main
```

---

## 2. 代码修改流程

### 2.1 修改前检查清单
- [ ] 已获取最新代码（`git pull`）
- [ ] 工作目录干净（`git status`）
- [ ] 理解待修改代码的业务逻辑
- [ ] 确认修改不会影响其他模块

### 2.2 修改中规范
- **最小化修改**：只修改必要的代码，避免无关改动
- **代码风格**：遵循项目现有代码风格
- **注释更新**：如果修改了功能逻辑，同步更新相关注释
- **测试验证**：修改后必须运行相关测试

### 2.3 修改后验证
- [ ] 编译通过（`mvn clean compile`）
- [ ] 单元测试通过（`mvn test`）
- [ ] 无语法错误和警告
- [ ] 代码格式符合规范

---

## 3. 依赖管理规范

### 3.1 新增依赖
- 优先使用项目已有的依赖版本（查看 parent pom）
- 版本号统一在 `pom.xml` 的 `properties` 中定义
- 通用依赖在根 `pom.xml` 管理，模块独有依赖在模块 `pom.xml` 管理

### 3.2 修改依赖
- 修改前确认影响范围
- 修改后执行 `mvn clean install` 验证

---

## 4. 错误处理规范

### 4.1 全局异常处理
- 确保全局异常处理器能正确处理不同返回类型
- SSE 流式请求需要特殊处理，返回 `SseEmitter` 而非 `ResponseEntity`
- 错误信息需要清晰、友好

### 4.2 日志记录
- 异常发生时记录详细日志（包含堆栈信息）
- 敏感信息不记录到日志中

---

## 5. 分支管理规范

### 5.1 分支命名
```
feature/功能名称          # 新功能开发
bugfix/问题描述           # Bug修复
hotfix/紧急修复           # 紧急线上修复
release/版本号            # 版本发布
```

### 5.2 代码合并
- 修改完成后推送到远程分支
- 创建 Pull Request 进行代码审查
- 审查通过后合并到主分支

---

## 6. 常见问题及解决方案

### 问题1：编译报错 - 无法访问类/方法
**原因**：依赖缺失或版本冲突
**解决**：
1. 检查 `pom.xml` 依赖配置
2. 执行 `mvn clean install -U` 更新依赖
3. 确认依赖版本正确

### 问题2：类型转换异常
**原因**：返回类型不匹配
**解决**：
1. 检查控制器方法返回类型
2. 确认全局异常处理器支持该返回类型
3. 为不同返回类型添加对应的异常处理

### 问题3：API 请求认证失败（401）
**原因**：API Key 未配置或配置错误
**解决**：
1. 设置环境变量 `QWEN_API_KEY=your-api-key`
2. 或修改 `application.yml` 配置文件

---

## 7. 附录：常用命令

```bash
# 编译项目
mvn clean compile

# 运行测试
mvn test

# 打包项目
mvn clean package

# 更新依赖
mvn clean install -U

# 查看依赖树
mvn dependency:tree
```

---

**最后更新**: 2026-05-15  
**适用范围**: AI Practice 项目所有模块